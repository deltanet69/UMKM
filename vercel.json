{
  "buildCommand": "node ./scripts/patch-router.js && npx expo export -p web -c",
  "installCommand": "npm install",
  "outputDirectory": "dist",
  "framework": null,
  "rewrites": [
    { "source": "/((?!.*\\.).*)", "destination": "/index.html" }
  ]
}
